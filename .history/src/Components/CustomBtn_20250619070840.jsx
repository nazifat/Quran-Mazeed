import React from 'react';
import { Link } from 'react-router-dom';

const CustomBtn = () => {
    return (
        <Link className="btn btn-outline custom-btn">Contact Us</Link>

    );
};

export default CustomBtn;